import { ExtractYearPipe } from './extract-year.pipe';

describe('ExtractYearPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractYearPipe();
    expect(pipe).toBeTruthy();
  });
});
